Suspend On
^!l::Suspend
#Hotstring EndChars `t `t
<% notation_to_unicode_char.keys.sort.each do |latex| %>:c:<%= latex.gsub(/`/, "``") %>::
Send {U+<%= notation_to_unicode_char[latex].ord.to_s(16).upcase.rjust(4, "0") %>}
return
<% end %>


