(quail-define-package
 "latex" "LaTeX" "LTX" t
 "Use LaTeX notation to enter mathematical symbols.")

(quail-define-rules
<% latex_to_unicode.keys.sort.each do |latex| %>("<%= escape(latex)%> " ?<%= latex_to_unicode[latex] %>)
<% end %>)

(provide 'latex)
